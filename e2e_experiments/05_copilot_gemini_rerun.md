Edit and additionally rerun the experiments in `blade-evaluation-copilot` to use Gemini 3.1 (Preview) instead of Claude sonnet.

- Once the experiments are setup, run the experiments.
- First, run a single seed for 3 datasets, then proceed to run the full set of experiments across all seeds and datasets
- If this succeeds, edit and run the experiments in `blade-evaluation-copilot-stability` to also use Gemini 3.1 (Preview)

Finally update the results in `../paper-imodels-agentic/content.tex` (in particular fig 4a, fig 4b, fig 4 caption + writing in sec 4.2 and the end of 4.1).
